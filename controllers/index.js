const buildingController = require('./building')
const reviewController = require('./review')
const styleController = require('./style')

module.exports = {
  buildingController,
  reviewController,
  styleController
}
